### Fixed

- Fixed remaining compile errors in PushPayloadTests and DecisionAnswerRequestTests, restored the single_select wire-contract assertion, and made `deviceId` on `PushRegistrar` settable from tests.
