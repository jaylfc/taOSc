### Fixed

- Fixed three compile errors in `DecisionNotificationHandler` and corrected the answer body contract so `add_note` no longer posts a fabricated answer and `quick_reply` sends the typed text as `value` without `other_value`.
