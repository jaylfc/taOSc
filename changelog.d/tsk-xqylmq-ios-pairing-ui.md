### Added
- Login method chooser (LAN and taOSgo) on first run.
- LAN pairing grant flow with verify code display and poll state machine.
- taOSgo sign-in path with tailnet join guidance and shared grant flow.
- Keychain storage for scoped_token; token is never written to UserDefaults.
